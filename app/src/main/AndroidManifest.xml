<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".view.base.FoodieApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/baseline_food_bank_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/baseline_food_bank_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodie"
        tools:targetApi="31">
        <activity
            android:name=".view.ui.checkout.CheckOutActivity"
            android:exported="false" />
        <activity
            android:name=".view.ui.home.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.ui.beverages.BeveragesActivity"
            android:exported="true"></activity>
    </application>

</manifest>